import {describe,it,expect} from 'vitest';import {Ball} from '../src/game/Ball.js';const s={start:{x:0,y:3.2,z:-8},landing:{x:1.5,z:7},durationMs:2400,arcHeight:3.5,drift:.2,floatOffset:-.15};describe('ball',()=>{it('starts and ends correctly',()=>{const b=new Ball();b.start(s);expect(b.getPosition(0)).toEqual(s.start);expect(b.getPosition(1).x).toBeCloseTo(1.5);expect(b.getPosition(1).y).toBeCloseTo(.75);});});
