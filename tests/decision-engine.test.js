import {describe,it,expect} from 'vitest';import {decideOwnership} from '../src/game/DecisionEngine.js';const rs=[{id:'left',x:-3,z:6.8},{id:'middle',x:0,z:7.2},{id:'right',x:3,z:6.8}];describe('ownership',()=>{it('assigns centered ball to middle',()=>expect(decideOwnership({landing:{x:0,z:7},receivers:rs}).expectedCall).toBe('mine'));it('assigns left ball away',()=>expect(decideOwnership({landing:{x:-3,z:6.8},receivers:rs}).ownerId).toBe('left'));});
