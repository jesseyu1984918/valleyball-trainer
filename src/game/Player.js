import * as THREE from 'three';
import { COURT } from '../config.js';
import { clamp, normalize } from '../math/vector2.js';
export class Player{constructor(scene,{id='middle',x=0,z=7.2,color=0x36c2ff}={}){this.id=id;this.x=x;this.z=z;this.velocity={x:0,z:0};this.speed=0;this.mesh=new THREE.Mesh(new THREE.CapsuleGeometry(.28,.9,8,16),new THREE.MeshStandardMaterial({color}));this.mesh.position.set(x,.75,z);this.mesh.castShadow=true;scene.add(this.mesh);}reset({x,z}){this.x=x;this.z=z;this.velocity={x:0,z:0};this.speed=0;this.sync();}update(input,dt){const d=normalize(input);const speed=3.3;this.velocity={x:d.x*speed,z:d.z*speed};this.speed=Math.hypot(this.velocity.x,this.velocity.z);this.x=clamp(this.x+this.velocity.x*dt,COURT.receiveMinX,COURT.receiveMaxX);this.z=clamp(this.z+this.velocity.z*dt,COURT.playableMinZ,COURT.playableMaxZ);this.sync();}sync(){this.mesh.position.set(this.x,.75,this.z);}snapshot(){return{id:this.id,x:this.x,z:this.z,velocity:{...this.velocity},speed:this.speed};}}
